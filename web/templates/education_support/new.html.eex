<h2>New education support</h2>

<%= render "form.html", changeset: @changeset,
                        action: education_support_path(@conn, :create) %>

<%= link "Back", to: education_support_path(@conn, :index) %>
