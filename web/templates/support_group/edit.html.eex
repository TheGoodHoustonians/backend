<h2>Edit support group</h2>

<%= render "form.html", changeset: @changeset,
                        action: support_group_path(@conn, :update, @support_group) %>

<%= link "Back", to: support_group_path(@conn, :index) %>
